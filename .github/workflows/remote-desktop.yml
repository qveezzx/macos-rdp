name: macOS Remote Desktop
on: 
  workflow_dispatch:

jobs:
  vnc-access:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable macOS Screen Sharing (VNC)
        run: |
          # Turn on the Screen Sharing service
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -restart -agent -privs -all
          
          # Set the VNC password to 'runner'
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -configure -clientopts -setvncpasswd -vncpasswd runner

      - name: Setup Tmate session
        uses: mxschmitt/action-tmate@v3
        timeout-minutes: 360
        with:
          # This ensures the session stays open even if you close your browser
          limit-access-to-actor: false